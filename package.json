{
  "name": "frontend-vue-test",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && cp 404.html dist/ && cp -r api/ dist/api/ && gh-pages -d dist"
  },
  "dependencies": {
    "@mdi/font": "^7.4.47",
    "vue": "^3.5.12",
    "vue-router": "^4.5.0",
    "vuetify": "^3.7.6"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.1.4",
    "gh-pages": "^6.2.0",
    "vite": "^5.4.10"
  }
}
